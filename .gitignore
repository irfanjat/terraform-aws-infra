# Terraform state — never commit these
*.tfstate
*.tfstate.backup
.terraform/
.terraform.lock.hcl

# tfvars has DB password — never commit
terraform.tfvars

# Crash logs
crash.log
crash.*.log

# Override files
override.tf
override.tf.json
